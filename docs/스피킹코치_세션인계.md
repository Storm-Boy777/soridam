# 스피킹 코치 v5 — 세션 인계 문서

> 다음 세션 시작 시 가장 먼저 읽기. 현재 상태 / 완료한 작업 / 다음 단계 / 검증 시나리오 / 트러블슈팅 풀 정리.

**최종 갱신**: 2026-05-17
**현재 커밋**: `8f22b9a` (분사구문+ModelAnswer 시각화) — 마이그 081/082/083 + TTS 듣기 버튼 + EF Layer 3 합성 미커밋

## 🚨 2026-05-17 — 3-layer 구조 전환 (필독)

마이그 081 24 row 적재 후 dogfooding 검증(지형 IH 답변)에서 발견된 구조적 한계:
- 081의 `example_model_answer`가 그룹당 1 토픽(환경=재활용)만 → 학생 지형 답변 시 자료 #12 D 지형 풀 모범 핵심 표현 약화 인용.

→ **3-layer 구조로 전환** (자료 #21 강사 1:1 코칭 etalon 기반):

```
Layer 1 (등급 헌법, 마이그 078)   ←─ common_{level} × 6 row
Layer 2 (그룹 격상 카드, 마이그 081) ←─ description_random_{group}_{level} × 24 row
Layer 3 (토픽 만능 표현 SSOT, 마이그 082) ←─ coaching_topic_skeletons × 12 row (신규)
```

EF coaching-evaluate가 3-layer 동시 fetch + assembleUserPrompt에 Layer C 섹션 주입. 자료 #21 핵심 원칙(학생 답변 골격 유지 + 슬롯 카피 의무) coaching_system_v1에 통합(마이그 083, length 7574 → 9974).

---

## 🎯 현재 상태 한 줄 요약

> **스피킹 코치 v5 = 자료 #1~#17 + Common SSOT 2-layer spec 기반 백지 재설계. 묘사(description) 유형 1차 MVP 작동 검증 완료(92/100). 권한 시스템 적용. 다음 세션은 묘사 외 유형(routine·comparison·past·adv·rp) spec 작성.**

---

## 📋 완료된 작업 (Phase 1 — 묘사 유형 MVP)

### DB 마이그레이션 (074~080)

| # | 내용 |
|---|------|
| 074 | `coaching_specs` 테이블 신설 + `coaching_persona_settings` DROP + `coaching_sessions.target_level` 컬럼 + 기존 `coaching_*` 프롬프트 비활성화 + `coaching_system_v1` 신규 row (자료 #1~#17 통합 본문) |
| 075 | description × IL~AL 6 row 시드 (1차 MVP) |
| 076 | `coaching_system_v1` 강화 — STUDENT TEXT VERIFICATION + GRANULAR-FIRST + SELF-VALIDATION 9항목 |
| 077 | `description_IH` spec 재정렬 — 자료 #1 강사 etalon 1~3순위 그대로 |
| 078 | **common × IL~AL 6 row 시드 (등급별 공통 코칭 헌법 SSOT)** ← 핵심 |
| 079 | `coaching_access` 테이블 + RLS + `soridamhub@gmail.com` 초기 부여 |
| 080 | `common_IH.coaching_focus` 분사구문 의무 짚기 강화 (A 작업) |
| 081 | **돌발 4 그룹 × 6 등급 = 24 row 시드** (자료 #11~#14 풀 적재) |
| 082 | **`coaching_topic_skeletons` 테이블 신설 + 12 토픽 row** (Layer 3 SSOT — 자료 #11~#14 + #21 토픽 단위 만능 표현) |
| 083 | **`coaching_system_v1` 강화** — Layer 3 합성 원칙 + 자료 #21 코칭 etalon (학생 답변 골격 유지 + 슬롯 카피 의무 + 정량화/토론/듀얼리티 카드 의무) |

### EF (백지 재작성)

- `supabase/functions/coaching-evaluate/index.ts`:
  - `resolveSpecId(question)` — 4축 매칭 (survey_type × category × topic × question_type) + 돌발 4 그룹 분기
  - `fetchSpecPair(specId, targetLevel)` — **common(등급별 base) + type(유형별 차별) 두 row 합성 fetch**
  - `assembleSystemPrompt(spec)` — `coaching_system_v1` fetch + spec.tone_adjustment 미세조정
  - `assembleUserPrompt(spec, commonSpec, ...)` — Layer A(common) + Layer B(type) 2-layer 주입
  - Pass 1 단일 호출 (Pass 2 분리는 MVP 이후)
  - 기존 `buildUserPrompt` 함수 본문 전면 폐기

### SA / 타입 / UI

- `lib/actions/coaching.ts` — `startOrResumeSession`이 `profile.target_grade`를 `target_level`에 자동 캐시
- `lib/types/coaching.ts` — `CoachingSession.target_level` 필드 + `QUESTION_TYPE_LABELS` export
- `lib/constants/tables.ts` — `coaching_specs` / `coaching_access` 추가, 폐기된 `coaching_persona_settings` 제거
- `components/coaching/learn-room.tsx`:
  - **SessionMetaStrip** 신설 — Sticky 상단 (목표 등급 뱃지 + 토픽·유형 칩 + 회차 + 등급별 가이드 한 줄)
  - **ModelAnswer 시각화 강화** (C 작업) — `model_answer.changes`를 카테고리 자동 감지(어휘/문법/분사구문/표현/마무리/위치표지/구조) + 뱃지 + 그루핑

### 권한 시스템 (lecture_access 패턴 이식)

- `lib/auth.ts` — `hasCoachingAccess()` + `requireCoachingAccess()` 헬퍼
- `lib/actions/admin/coaching-access.ts` — 6 SA (stats/list/search/grant/revoke/bulkRevoke)
- `app/(admin)/admin/coaching-access/page.tsx` — 관리자 페이지 (lectures 패턴 그대로)
- 관리자 사이드바 메뉴 "스피킹 코치" (Sparkles 아이콘)
- 페이지 진입 차단: `/coaching` + `/coaching/learn/[sessionId]` 모두 `requireCoachingAccess()` 적용
- 네비바 노출 조건: `coachingAccessOnly` 플래그 (관리자 자동 통과)

### 설계 문서

- `docs/설계/스피킹코치_재설계.md` — §5 세부 설계 풀 정리 + §6 작업 범위 + §11.7.1~11.7.10 자료 #1~#17 분석 + §11.4.2 ACTFL FACT + §11.5 평가엔진 분석
- 자료 #1~#17 모두 분석 완료. 유형별 spec 시드는 단계적

---

## ✅ Dogfooding 검증 결과 (음악 IH 답변 3회)

> **자료 #1·#5 강사 etalon vs EF 비교 매트릭스 — 92/100 (자료 #11~#16 강의 자료 #1~#17 통합 system + common_IH spec 적용)**

| 영역 | 점수 |
|------|------|
| 학생 답변 정확 인용 (`exist from work` 같은 신선한 흠 짚음) | 100/100 |
| 짚는 단위 (단어·문장) | 95/100 |
| 짚는 순서 etalon 1~3순위 (어휘→Agreement→어색한 표현) | 100/100 |
| 통합답변 격상 적용 (위치별 표지·consisting of·분사구문) | 90/100 |
| 분사구문 짚기 일관성 | **80/100** (마이그 080 적용 후 95 기대) |
| Skeleton 충족 인식 (학생 사용한 표지 짚지 X) | 100/100 |
| LEVEL GATE (IH 학생에게 IH 가이드) | 100/100 |
| 강사 페르소나 톤 | 95/100 |

**확인된 강사식 정합 짚기**:
- `exist from work → get off work` (원어민 안 쓰는 표현)
- `Some of ... is Iruma → are` (Agreement)
- `this kind of songs → song` (단수 일치)
- `really 9회 반복 → genuinely/truly/incredibly` (어휘 격상)
- `all his songs → all of his songs` (자료 #1 강사 etalon 6번 정확 정합)
- `He is beautiful → His music is beautiful` (사람 묘사 어색)
- `closing tag 부재` 짚음
- 통합답변에 분사구문(`Watching him play`)·강조 표현(`without being bothered`)·위치별 표지(Q5 일관) 적용

---

## 🎯 다음 세션 작업 — Phase 2 (B 진행)

### 1차: 분사구문 강화 검증 (마이그 080)

**테스트**:
1. 음악 IH 새 답변 (또는 기존 답변 재실행)
2. EF 호출 → coaching_json.issues에 분사구문 흠이 짚이는지 확인
3. 학생 답변에 `When I + V + ing` 패턴 있으면 100% 짚어야 함

**기대**: 분사구문 일관성 80 → 95

### 2차: ModelAnswer UI 검증 (C 작업)

**테스트**:
1. 학습 룸 새로고침
2. 통합 답변 카드 하단의 "무엇이 바뀌었나" 영역에 카테고리 칩(어휘/문법/분사구문 등) + 그루핑 표시 확인
3. 변경 사항 수가 많을수록 카테고리별 압축 효과 확인

### 3차: B 진행 — 묘사 외 유형 spec 작성

**작업 순서 (단계적)**:

| 단계 | 유형 | 자료 | 산출물 | 상태 |
|------|------|------|------|------|
| 1 | description_random 4 그룹 × 6 등급 = **24 row** | 자료 #11~#14 (돌발 시사/환경/산업·기술/개인) | 마이그 081 | ✅ 2026-05-17 |
| 2 | rp_11 / rp_12 × 6 = **12 row** | 자료 #2/#6/#7 | 마이그 082 | ⏳ 다음 |
| 3 | past_childhood / past_recent / past_special × 6 = **18 row** | 자료 #8/#9/#10 | 마이그 083 | ⏳ |
| 4 | adv_14 / adv_15 × 6 = **12 row** | 자료 #3/#4/#15/#16/#17 | 마이그 084 | ⏳ |
| 5 | routine / comparison × 6 = **12 row** | DB 카탈로그 + 자료 #5/#8 합성 (강사 직접 자료 없음) | 마이그 085 | ⏳ |

→ 총 90 row 완성 시 14 유형 × 6 등급 spec 매트릭스 완료. 묘사 외 유형 코칭 즉시 활성화 가능.

**원칙**:
- common × 6 (등급별 헌법 SSOT, 마이그 078) **위에 차별 요소만** 적재
- 등급별 공통 요소는 common에 이미 있으므로 type spec은 슬림
- 유형 특화 요소만:
  - past_*: 과거 시제 컨트롤·만능 링크·과거-현재 대조 (자료 #8/#9/#10)
  - adv_14: 비교 만능 템플릿(자료 #15) + 변화 스마트폰 만능(자료 #16) + adv_14 양대 축
  - adv_15: 인플레이션+트럼프 만능 4단계(자료 #17) + 17 카테고리 단일 컨셉
  - rp_11/12: 친구·업체 페어 + 정중한 복잡 질문 + 12번 게이트키퍼
  - description_random_*: 4 그룹 차별(시사/환경 토론적 마무리/산업·기술 정량화/개인 양면 토론)

### 4차: self_intro 처리 확인

- 사용자 확정: self_intro 코칭 대상 X
- `coaching_specs`에 self_intro 행 포함 X (이미 누락 상태 유지)
- UI 코칭 토픽 노출에서 self_intro 제외 확인

### 5차: 다른 등급 description 정제 (선택)

- description_IL/IM1/IM2/IM3/AL spec 시드는 마이그 075에서 적재됐으나 마이그 078 common 시드 도입 이후 type spec과 common spec 일부 중복
- 차별 요소만 남기고 슬림화 가능 (다만 IH 검증 후 점진)

---

## 🧪 검증 시나리오 (다음 세션 시작 시)

### 시나리오 A: 분사구문 강화 검증

```sql
-- 1. 테스트 attempt 새로 생성 (음악 IH 답변, raw_transcript 직접 주입)
INSERT INTO coaching_sessions (user_id, question_type, topic, question_id, survey_type, target_level, status, attempt_count)
VALUES ('251b0655-6fd0-4566-bef2-57c07bb5dcd0', 'description', '음악', 'SEL_MUS_N_C2_01', '선택형', 'IH', 'active', 0)
RETURNING id;

-- 2. attempt INSERT (raw_transcript에 `When I watch him playing` 같은 패턴 포함된 답변)
-- 3. curl로 EF 호출 (Authorization: Bearer SERVICE_ROLE_KEY)
-- 4. SELECT coaching_json->'issues'에 "분사구문" 흠 짚이는지 확인
```

### 시나리오 B: UI 시각화 검증

1. https://soridamhub.com/coaching/learn/[sessionId] 접속 (권한 보유 계정)
2. 학습 룸의 "통합 답변" 카드 확인
3. 하단 "무엇이 바뀌었나" 영역에 카테고리 칩(어휘/문법/분사구문/표현/마무리 등) 노출 확인

### 시나리오 C: 권한 시스템 검증

1. 권한 없는 계정으로 `/coaching` 접속 시도 → 홈으로 redirect 확인
2. 네비바에 "스피킹 코치" 메뉴 숨김 확인
3. 관리자 `/admin/coaching-access`에서 이메일 검색 → 권한 부여 → 해당 계정 새로고침 → 메뉴 노출 확인

---

## 🔑 핵심 파일 경로

| 영역 | 파일 |
|------|------|
| EF | `supabase/functions/coaching-evaluate/index.ts` |
| EF preprocess (유지) | `supabase/functions/coaching-preprocess/index.ts` |
| SA | `lib/actions/coaching.ts` |
| 관리자 SA | `lib/actions/admin/coaching-access.ts` |
| 권한 헬퍼 | `lib/auth.ts` (hasCoachingAccess / requireCoachingAccess) |
| 타입 | `lib/types/coaching.ts`, `lib/types/admin.ts` |
| 학습 룸 UI | `components/coaching/learn-room.tsx` |
| 코칭 메인 페이지 | `app/(dashboard)/coaching/page.tsx` |
| 학습 룸 페이지 | `app/(immersive)/coaching/learn/[sessionId]/page.tsx` |
| 관리자 권한 페이지 | `app/(admin)/admin/coaching-access/page.tsx` |
| 설계 문서 | `docs/설계/스피킹코치_재설계.md` (3,200+ 줄) |
| 마이그레이션 | `supabase/migrations/074~080_*.sql` |

---

## ⚠️ 트러블슈팅

### 1. EF가 "spec 조회 실패" 에러 던질 때
- 원인: `common_{등급}` row 없음 (마이그 078 미적용 또는 등급 형식 불일치)
- 확인: `SELECT * FROM coaching_specs WHERE question_type='common' AND target_grade='IH';`
- 해결: 마이그 078 재적용 또는 등급 폴백 로직 점검

### 2. 학생이 "스피킹 코치" 메뉴 안 보임
- 원인: `coaching_access` 권한 없음
- 확인: `SELECT * FROM coaching_access WHERE user_id='{user_id}';`
- 해결: 관리자 `/admin/coaching-access`에서 부여

### 3. EF가 학생 답변에 있는 표현을 "부재"로 잡음 (예: transition)
- 원인: STUDENT TEXT VERIFICATION 원칙 위반
- 확인: `coaching_system_v1` 본문에 SELF-VALIDATION 1번 항목 확인
- 해결: 시스템 프롬프트 재강화 마이그 작성

### 4. issue 개수가 spec 범위 벗어남 (1~2개만 짚음)
- 원인: GRANULAR-FIRST 원칙 적용 부족
- 확인: `spec.issue_count_per_attempt` 값과 GPT 출력 비교
- 해결: 시스템 프롬프트의 `issue_count_per_attempt 절대 준수` 강화 또는 callGPTRetry 가드 도입

### 5. 분사구문 짚기 누락
- 원인: 마이그 080 미적용
- 확인: `SELECT coaching_focus FROM coaching_specs WHERE guide_id='common_IH';`에 "분사구문 기회 발견 시 의무 짚기" 명시 있는지
- 해결: 마이그 080 재적용

---

## 📊 spec 카탈로그 현재 상태

| spec_id | 등급 적재 | 상태 |
|---------|----------|------|
| `common` × 6 (IL~AL) | ✅ 6/6 | SSOT 완료 (마이그 078) |
| `description` × 6 | ✅ 6/6 | 1차 MVP 완료 (마이그 075). description_IH는 마이그 077·080으로 추가 강화 |
| `description_random_current_affairs` × 6 | ✅ 6/6 | 마이그 081 완료 (자료 #11 — 어휘 격상 매트릭스 + 디지털 대체 + 은행 풀 모범) |
| `description_random_environment` × 6 | ✅ 6/6 | 마이그 081 완료 (자료 #12 — Cohesive 6 카테고리 + 토론적 마무리 카드) |
| `description_random_industry_tech` × 6 | ✅ 6/6 | 마이그 081 완료 (자료 #13 — Breath of Vocabulary + 시장 점유율 정량화 + 한류 풀) |
| `description_random_personal` × 6 | ✅ 6/6 | 마이그 081 완료 (자료 #14 — 양면 토론 5단 + 자유시간 100% 스마트폰) |
| `routine` × 6 | ❌ 0/6 | 다음 세션 (DB 합성) |
| `comparison` × 6 | ❌ 0/6 | 다음 세션 (DB 합성 — adv_14 분리 원칙) |
| `past_childhood` × 6 | ❌ 0/6 | 다음 세션 (자료 #8) |
| `past_recent` × 6 | ❌ 0/6 | 다음 세션 (자료 #9) |
| `past_special` × 6 | ❌ 0/6 | 다음 세션 (자료 #10) |
| `adv_14` × 6 | ❌ 0/6 | 다음 세션 (자료 #15·#16) |
| `adv_15` × 6 | ❌ 0/6 | 다음 세션 (자료 #17) |
| `rp_11` × 6 | ❌ 0/6 | 다음 세션 (자료 #2·#6) |
| `rp_12` × 6 | ❌ 0/6 | 다음 세션 (자료 #2·#7) |
| ~~`self_intro`~~ | — | **포함 X** (사용자 확정) |

**총 진행률**: **36 / 90 row 완료 (40%)**. 다음 세션에 54 row 추가 작성 예정.

### 마이그 081 적재 결과 (검증 완료)

```
description_random_current_affairs | IL→AL  | model_len 222→1163 | focus_len 298→1231
description_random_environment     | IL→AL  | model_len 237→1247 | focus_len 260→1106
description_random_industry_tech   | IL→AL  | model_len 227→1368 | focus_len 232→1288
description_random_personal        | IL→AL  | model_len 186→1439 | focus_len 235→1331
```

24 row 모두 적재. 등급별 길이 점진 증가 패턴 일관. EF resolveSpecId는 이미 description_random_* 4개 매칭하도록 작성되어 있어 **재배포 불필요** — 돌발 13 토픽 질문 입력 시 즉시 작동.

---

## 🔗 환경 정보 (재확인)

- DB: `fkkdbnebsaecjpqhhdvl` (Supabase Northeast Asia)
- Storage 버킷: `coaching-recordings`
- 테스트 계정: `soridamhub@gmail.com` (target_grade=IH, coaching_access ✅)
- 관리자 계정: `js777hd@gmail.com` (target_grade=AL, admin)
- EF 배포: `--no-verify-jwt` 플래그 필수 (프로젝트 공통 정책)
- 시스템 프롬프트 위치: `ai_prompt_templates.coaching_system_v1` (현재 7,574자)

---

## ✍️ 다음 세션 첫 메시지 추천

```
스피킹 코치 v5 다음 단계 진행하자.
docs/스피킹코치_세션인계.md 먼저 읽고,
1차로 마이그 080 분사구문 강화 + UI ModelAnswer 시각화 검증부터.
검증 OK면 B 진행 (다음 우선순위: description_random 4 그룹 × 6 등급 = 24 row).
```
