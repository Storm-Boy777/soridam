// Talklish · 진입 페이지 — 월/수/금 카드 선택
// layout.tsx에서 폰트 변수 + 권한 게이트 일원화되어 있으므로 본 파일은 컴포넌트만 렌더.

import { TalklishEntryPage } from "@/components/study-group/talklish/entry-page";

export default function StudyGroupEntryPage() {
  return <TalklishEntryPage />;
}
